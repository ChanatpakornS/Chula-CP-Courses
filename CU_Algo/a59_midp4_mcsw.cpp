#include <iostream>
#include <limits>
#include <algorithm>

using namespace std;

int main() {

    int n, w;
    int best_sum = -numeric_limits<int>::infinity();
    int curr_sum = 0;
    int tmp;
    cin >> n >> w;

    for (int i=0 ; i < n ; ++i) {
        cin >> tmp;
        curr_sum += 
    }

    return 0;
}